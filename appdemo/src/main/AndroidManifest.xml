<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ AndroidBottomSheetDialog by Stephanus Dai (戴偉峯)
  ~ @link http://www.stephanusdai.web.id/
  ~ @mail wiefunk@stephanusdai.web.id
  ~ @copyright Copyright (c) 2023 Stephanus Bagus Saputra
  ~ @license http://stephanusdai.web.id/license.html
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="id.web.stephanusdai.appdemo" android:versionName="1.0.1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HekstaroidDemo"
        android:configChanges="screenLayout|uiMode|orientation|screenSize"
        tools:targetApi="27">
        <activity
            android:name=".NetworkMonitorActivity"
            android:configChanges="screenLayout|uiMode|orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".BottomNoticeDialogActivity"
            android:configChanges="screenLayout|uiMode|orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".FullNormalScreenActivity"
            android:configChanges="screenLayout|uiMode|orientation|screenSize"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:configChanges="screenLayout|uiMode|orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>